import React from 'react';

const Social = (props) => {
    const {name} = props
  return (
    
 
        <p className="ml-4  md:ml-12 text-red-900 text-2xl font-semibold ">
         {name}
        </p>
    
  
    );
};

export default Social;
